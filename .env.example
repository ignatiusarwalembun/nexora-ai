# Optional local overrides
PORT=5500
NODE_ENV=development

# Recommended in production. Use a long random secret.
MABA_MASTER_KEY=replace-with-a-long-random-secret

# Optional. Default is ./data
# Railway volume suggestion: /app/data
MABA_DATA_DIR=./data
